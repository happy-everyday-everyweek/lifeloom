# 项目词汇表

Lifeloom——插件化的模拟人生类游戏（开源）。本文件只收录本项目特有的词汇，作为全项目统一用词；不写实现细节。

## Language

### 系统与架构

**核心**：承载并调度插件的宿主；自身不含任何玩法。
_Avoid_：引擎、内核、核心插件

**插件**：可安装、卸载、整体替换的功能单元；携带签名，由机制组成。
_Avoid_：模组、MOD、扩展包

**系统插件**：随游戏分发的官方插件；出厂玩法面向纯自由。已知底层成员：存档、UI、动作、钩子管理、地图、NPC、AI、跨端流转、权限 等，清单持续补充。
_Avoid_：核心插件、内置插件

**底层插件**：系统插件中只提供标准化能力、不含任何玩法的那一层；首批成员：UI、动作、钩子管理、存档、地图、NPC、AI、跨端流转、权限。
_Avoid_：基础插件

**三方插件**：系统之外来源、非系统签名的插件；其跨机制修改需要用户授权。
_Avoid_：第三方插件、外部插件

**机制**：插件内的最小功能与权限单元；拥有全局唯一 ID（形似包名），内含若干钩子。
_Avoid_：模块、组件

**钩子**：机制内的挂载点，拥有唯一 ID，指向一段代码文件。
_Avoid_：回调、监听器

**运行时装载**：游戏运行中安装新插件并立即生效。
_Avoid_：动态安装

**热装载**：游戏运行中整体替换、升级已装插件，数据不断档。
_Avoid_：热更新、热补丁

**存档**：世界状态的持久化；由系统插件（存档插件）提供。
_Avoid_：进度

**跨端流转**：把存档、插件、远程访问等从一个端流转到另一个端；由独立的跨端流转插件负责；同一局域网下要求即时流畅。
_Avoid_：云同步

### 世界与实例

**实例**：世界中的个体单元；分生物与物体；均可附着参数（参数也可附着到机制）。
_Avoid_：实体、对象

**分类**：实例的归属层级；顶层固定为人、动物、物品，插件不能新增顶层分类。
_Avoid_：种类

**种**：实例的分类模板，规定该种实例可拥有的参数与动作。
_Avoid_：类型、物种

**生物**：可执行动作的实例大类，含人与动物。
_Avoid_：活物

**人**：生物中的一类；所有人的可执行动作与玩家所扮演的人一致；可以被“进入”为被体验实例。
_Avoid_：人类、角色、玩家角色

**动物**：生物中的一类；可执行动作，但动作集与人不一致；不能被“进入”为被体验实例。

**物体**：不能执行动作的实例，只能作为动作的附着者（例如一瓶饮料）。
_Avoid_：道具、物品

**NPC**：非玩家操控的实例；行为由 NPC 插件管理；与玩家时间严格同步，仅在时间流逝时活动。

**被体验实例**：用户当前“作为”其来体验的实例；开局时由用户创建一个新的人实例作为自身；只有人可担任；中途“进入/切换”不由系统插件提供，三方插件可提供。
_Avoid_：主角、玩家角色

### 参数与动作

**参数**：描述实例或机制状态的数据；分基础参数与派生参数。注册新参数不需要授权。
_Avoid_：属性、数值

**基础参数**：可以被直接修改的参数。
_Avoid_：自由参数、原始参数

**派生参数**：由基础参数计算得出的只读参数；用于判定动作能否发生以及动作结果。
_Avoid_：计算参数、间接参数

**动作**：附着在实例上的交互入口；本质是钩子，触发逻辑并修改参数；只有生物能执行动作。
_Avoid_：行为、能力、操作

**动作时长**：动作声明的持续时间；可变、由插件提供；游戏内时间在动作执行期间流逝。
_Avoid_：耗时

**发出者**：执行动作的实例（“喝”的发出者是喝水的人）。
_Avoid_：执行方、触发者

**弹性动作**：玩家用自然语言向 AI 描述想要的动作，由 AI 解析后直接修改参数来达成。
_Avoid_：自由动作、临时动作

**游戏内时间**：由插件提供；仅在动作执行时按动作时长流逝；可调倍率，无动作则静止。
_Avoid_：时钟、世界时间

### 权限与信任

**跨机制修改**：插件对自身之外机制的改动（参数、钩子、代码文件）；一律需要用户授权。
_Avoid_：插件修改、劫持

**权限插件**：系统插件之一；对所有非系统插件的跨边界操作作出放行决定，并负责向用户提示。
_Avoid_：权限中心、权限模块

**闸门**：核心保留的极薄原语；只把非系统插件的操作请求转发给决策者（如权限插件），自身不作放行决定。
_Avoid_：权限校验器

**冲突检测**：检测插件之间的冲突（如对同一钩子的争用）。
_Avoid_：兼容性检查

**签名**：插件携带的凭证；用于辨识系统、三方身份并校验完整性。
_Avoid_：证书、指纹

### 玩法与 AI

**纯自由沙盒**：系统插件的出厂玩法：无固定目标、自由行动；目标类玩法由玩家装载的插件提供。
_Avoid_：沙盒模式

**AI**：分布在系统插件各处的辅助能力（例如动作插件的弹性动作）；经在线接口访问；仅为辅助，游戏不是纯 AI 游戏。
_Avoid_：助手、机器人

**AI 插件**：系统插件之一；不承载具体 AI，只作为向用户自己配置的服务端发起 AI 请求的代理——避免三方插件直接接触用户密钥。
